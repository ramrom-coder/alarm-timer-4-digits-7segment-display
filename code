//buzzer= pin 12
// D1 - 8
// A = 1
// F=2
// D2= 9
// D3 =10
//B = 0
//E=3
//D=4
//DP =5
//C =6
// G= 7
// d4 =11.
int D1=10;
int A =3;
int F= 4;
int D2= 11;
int D3= 12;
int B =2;
int E= 5;
int D =6;
int DP= 7;
int C =8;
int G =9;
int D4= 13;
int userinput;
int buzzPin= A1;
#include "SevSeg.h"
SevSeg sevseg;
byte numDigits =4;
byte digitPins[]= {D1,D2,D3,D4};
byte segmentPins[]={A,B,C,D,E,F,G,DP};
bool resistorsOnSegments= true;
byte hardwareConfig= COMMON_CATHODE;
int delayTime = 5;
 void setup ()  {
  sevseg.begin(hardwareConfig,numDigits,digitPins,segmentPins,resistorsOnSegments);
  sevseg.setBrightness(90);
  Serial.begin(9600);

 }
 void loop()
  {
// DIGIT 1
  //digitalWrite(A,HIGH);
  //digitalWrite(B,HIGH);
  //digitalWrite(C,HIGH);
 Serial.println("Set a timer for seconds(Maximum 9999):");
while(Serial.available()==0){

}
userinput = Serial.parseInt();
if(userinput <=9999 && userinput > 0){
  //Serial.println(userinput)
  while(userinput > 0){
    sevseg.setNumber(userinput);
  //1 second delay with refresh every 5ms
  //200 *5 = 1 sec
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
    sevseg.refreshDisplay();
  delay(delayTime);
  sevseg.refreshDisplay();
  userinput= userinput -1 ;
  sevseg.refreshDisplay();

  //remove the extra second

  }
  analogWrite(buzzPin, 255);
  delay(5000);
  analogWrite(buzzPin,0);
  sevseg.blank(); //clears out display
}else{
Serial.println("Sorry the limited number is 999. Please try again.");}
 }
 
